<div class="container">
<div class="row">
  <% if @book.errors.any? %>
    <div id="errors_explanation" class="alert alert-danger">
      <p>error</p>
        <%= @book.errors.count %>件のエラーが発生しました
        <ul>
          <% @book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
        <% end %>
        </ul>
    </div>
  <% end %>
  <div class="col-lg-4" style="background-color: #f0fff0">
  <div class="side-bar">
      <%= render 'layouts/bookers2', user:@user ,new_book:@new_book %>
  </div>
  </div>
  
  <div class="col-lg-8">
      <div class="user-title">
          <h4>Users</h4>
      </div>
      <table class="table table-striped">
          <tr>
            <th>image</th>
            <th>name</th>
          </tr>
          <tr>
             <% @users.each do |user| %>
            <th><%= user.profile_image %></th>
            <th><%= user.name %></th>
            <th><%= link_to "Show", user_path(user.id) %></th>
          </tr>
            <% end %>
      </table>
  </div>
</div>
</div>